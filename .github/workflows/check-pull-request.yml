---
name: Check Pull Request

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  check-pull-request:
    uses: spacedog/demo-terraform/.github/workflows/terraform-plan.yml@61350ba56dac2ab975882e12fec32b8ff595c0f6
    with:
      post_comment: true
      environment: development
      terraform_version: latest
